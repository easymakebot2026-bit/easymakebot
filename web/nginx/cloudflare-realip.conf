# Placeholder — regenerate on the server (see docs/10-deploy-runbook.md §A2):
#
#   { for u in https://www.cloudflare.com/ips-v4 https://www.cloudflare.com/ips-v6; do
#       curl -s $u | sed 's/^/set_real_ip_from /; s/$/;/'; done
#     echo 'real_ip_header CF-Connecting-IP;'; } > nginx/cloudflare-realip.conf
#
# Until then, nginx sees Cloudflare's IP as the client (rate-limits still work,
# just per-edge-node rather than per-visitor).
real_ip_header CF-Connecting-IP;
